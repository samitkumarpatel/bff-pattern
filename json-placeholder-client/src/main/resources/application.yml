spring:
  application:
    name: json-placeholder-client
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000

server:
  port: 8082

management:
  endpoint:
    health:
      access: read_only
      probes:
        enabled: true
