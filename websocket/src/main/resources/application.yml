spring:
  application:
    name: websocket
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000

server:
  port: 8084

logging:
  level:
    web: debug
    org.springframework.security: debug
    websockets: debug